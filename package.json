{
  "name": "armonia",
  "version": "0.0.1",
  "description": "Armonia",
  "dependencies": {
    "gulp": "^3.5.6",
    "gulp-concat": "^2.2.0",
    "gulp-minify-css": "^0.3.0",
    "gulp-rename": "^1.2.0",
    "gulp-sass": "^2.0.4",
    "bower": "^1.3.3",
    "shelljs": "^0.3.0",
    "http-server": "^0.6.1"
  },
  "devDependencies": {
    
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server www/ -a localhost -p 8000 -c-1 -d false",
    "pretest": "npm install"
  }
}
